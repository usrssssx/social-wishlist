databases:
  - name: swl-db
    databaseName: wishlist
    user: wishlist
    plan: basic-256mb

services:
  - type: web
    name: swl-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    plan: starter
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: swl-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        sync: false
      - key: APP_BASE_URL
        sync: false
      - key: ENVIRONMENT
        value: production
      - key: ALERTS_TEST_TOKEN
        sync: false
      - key: REDIS_URL
        sync: false
      - key: REALTIME_REDIS_CHANNEL
        value: swl:realtime:events
      - key: MIN_CONTRIBUTION_AMOUNT
        value: 100
      - key: CAPTCHA_PROVIDER
        value: turnstile
      - key: CAPTCHA_SECRET_KEY
        sync: false
      - key: CAPTCHA_VERIFY_URL
        value: https://challenges.cloudflare.com/turnstile/v0/siteverify
      - key: CAPTCHA_EXPECTED_HOSTNAME
        sync: false
      - key: ALLOW_TEST_CAPTCHA_IN_PRODUCTION
        value: false
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_WEBHOOK_SECRET
        sync: false
      - key: EMAIL_SEND_RETRIES
        value: 3
      - key: EMAIL_SEND_RETRY_BACKOFF_SECONDS
        value: 1.0
      - key: EMAIL_SEND_TIMEOUT_SECONDS
        value: 15.0

  - type: web
    name: swl-frontend
    env: node
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm run start
    plan: starter
    envVars:
      - key: NEXT_PUBLIC_API_URL
        sync: false
      - key: NEXT_PUBLIC_TURNSTILE_SITE_KEY
        sync: false
